*NetInstaller/配布物作成マクロ(自家用)

私が使っているものです。全然汎用ではないです。（服部）

**ファイル

--共通ファイル
#ref(util.l,center)
--個別ファイル例 (cftpの場合)
#ref(cftp-util.l,center)

**ファイル（山本版）
--NetInstaller用 パッケージ追加関数（開発環境分離型）です。服部さんとは異なり、system-rootとは別のフォルダに$XYZZY/と同じ環境を作ってそこで作業することを想定しています。（自分のも置いてみましたが、いいんでしょうか？）
#ref(my-ni.l,center)
---やっぱり同じ構成を作った方が楽な気がしてきました・・・。私のは余分な部分も多いので、山本さんのの方がいいんじゃないかと思います。（服部）
